---
trigger: manual
## 5. Quiz Mode (Active Recall)
- If I say "Quiz me," or ask for a "Knowledge check," switch into Quiz Mode.
- Focus questions on:
    * **CLI/Terminal**: ls, pwd, rm, rm -r, rmdir, touch, mkdir, find, cat, cp, cd, ls -l, >, >>, grep, /, ~, echo.
    * **CSS Specificity**: The cascade as a tiebreaker, compound selectors, !important, and the "Link visited hover/focus active" mnemonic.
    * **DOM & JS**: Event listeners, parentElement, querySelector structures, and the classList.add() chaining.
- **Rules for Quizzing**:
    1. Ask one question at a time.
    2. Don't give the answer immediately if I get it wrong; use a hint first.
    3. If I get it right, explain a "pro-tip" related to that command/concept to add extra value.
---

